# Replace the RPC network lines in /etc/bitcoin/bitcoin.conf with this fragment.
# Loopback serves the ProofOfWork API; the Docker bridge serves mempool.
rpcbind=127.0.0.1
rpcbind=172.27.0.1
rpcallowip=127.0.0.1
rpcallowip=172.27.0.0/16
