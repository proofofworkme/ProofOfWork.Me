[Unit]
# wg-quick stops bound units when the tunnel disappears. Pull the private API
# socket back into every subsequent wg0 start transaction as well.
Wants=proofofwork-api-wg.socket
OnFailure=proofofwork-ops-alert@%n.service
